export default function BookingSummary({ booking, total, compact = false }) {
  return <aside className={`booking-summary ${compact ? 'compact' : ''}`}><div className="summary-heading"><span>ORDER SUMMARY</span>{booking.service && <span className="summary-dot">●</span>}</div><h3>{booking.service?.name || 'Your laundry order'}</h3><div className="summary-line"><span>Quantity</span><strong>{booking.quantity || 1} {booking.service?.id === 'ironing' ? 'sets' : 'bags'}</strong></div>{booking.date && <div className="summary-line"><span>Pickup</span><strong>{booking.date} · {booking.time || 'Choose a time'}</strong></div>}{booking.address?.street && <div className="summary-line"><span>Address</span><strong>{booking.address.street}, {booking.address.city}</strong></div>}<div className="summary-total"><span>Total</span><strong>${total.toFixed(2)}</strong></div>{!compact && <p className="summary-footnote">Final price includes pickup and delivery.</p>}</aside>;
}